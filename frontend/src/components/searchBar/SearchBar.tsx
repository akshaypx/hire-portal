import { Input } from "@/components/ui/input";

const SearchBar = () => {
  return (
    <div className="w-1/3">
      <Input placeholder="Search for job posts, companies, location" />
    </div>
  );
};

export default SearchBar;
